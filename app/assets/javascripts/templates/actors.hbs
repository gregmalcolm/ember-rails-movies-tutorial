<h1>Actors</h1>
<div class="row">
  <div class="col-sm-3">
    <ul style="list-style:none; ">
      {{#each actor in content}}
      <li>{{link-to actor.name "actors.show" actor}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="col-sm-9">
    {{outlet}}
  </div>
</div>
